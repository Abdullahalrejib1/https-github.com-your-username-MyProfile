# إعداد قاعدة البيانات - Users Table

## ✅ الربط التلقائي

الخادم (`server.js`) مربوط بقاعدة البيانات تلقائياً. عند تشغيل الخادم لأول مرة، سيتم:

1. ✅ إنشاء جدول `Users` تلقائياً
2. ✅ إنشاء Index على البريد الإلكتروني
3. ✅ إنشاء مستخدم admin افتراضي

## 📋 طرق الربط

### الطريقة 1: تلقائي (موصى بها) ⭐

عند تشغيل الخادم:
```bash
npm start
```

سيتم إنشاء الجدول تلقائياً إذا لم يكن موجوداً.

### الطريقة 2: يدوياً باستخدام SQL Server Management Studio

1. افتح **SQL Server Management Studio (SSMS)**
2. اتصل بالخادم: `ABDULLAH\SQLEXPRESS`
3. افتح ملف `database/users.sql`
4. تأكد من تحديد قاعدة البيانات `MyProfileDB`
5. اضغط **F5** أو **Execute** لتنفيذ الـ script

### الطريقة 3: باستخدام سكريبت PowerShell

```powershell
cd database
.\run-sql.ps1
```

### الطريقة 4: باستخدام سكريبت Batch

```cmd
cd database
run-sql.bat
```

### الطريقة 5: باستخدام sqlcmd من سطر الأوامر

```bash
sqlcmd -S ABDULLAH\SQLEXPRESS -d MyProfileDB -i database\users.sql -E
```

## 🔍 التحقق من الربط

بعد التنفيذ، يمكنك التحقق من أن الجدول تم إنشاؤه:

```sql
-- التحقق من وجود الجدول
SELECT * FROM Users;

-- التحقق من المستخدم الافتراضي
SELECT id, email, role, createdAt FROM Users WHERE email = 'admin@admin.com';
```

## 🔐 بيانات تسجيل الدخول

بعد إنشاء الجدول، يمكنك تسجيل الدخول باستخدام:

- **البريد الإلكتروني:** `admin@admin.com`
- **كلمة المرور:** `admin123`

## 📊 هيكل الجدول

```
Users
├── id (INT, PRIMARY KEY, IDENTITY)
├── email (NVARCHAR(255), UNIQUE, NOT NULL)
├── password (NVARCHAR(255), NOT NULL) - مشفر
├── role (NVARCHAR(50), DEFAULT 'admin')
├── createdAt (DATETIME, DEFAULT GETDATE())
└── updatedAt (DATETIME, DEFAULT GETDATE())
```

## ⚠️ ملاحظات مهمة

1. **التشفير:** كلمات المرور مشفرة باستخدام hash function بسيط
2. **الإنتاج:** في بيئة الإنتاج، استخدم `bcrypt` أو مكتبة تشفير أقوى
3. **الأمان:** احمِ قاعدة البيانات ولا تعرّض كلمات المرور

## 🛠️ إضافة مستخدم جديد

يمكنك إضافة مستخدم جديد من خلال SQL:

```sql
-- حساب hash من خلال server.js أولاً
-- ثم استخدم القيمة المشفرة
INSERT INTO Users (email, password, role) 
VALUES ('newadmin@example.com', 'hashed_password_here', 'admin');
```

أو يمكنك إضافة endpoint في `server.js` لإنشاء مستخدمين جدد.

## ✅ التحقق من نجاح الربط

عند تشغيل الخادم، يجب أن ترى هذه الرسائل:

```
✅ Connected to SQL Server database
✅ PortfolioData table ready
✅ Users table ready
✅ Users table index ready
✅ Default admin user created
   Email: admin@admin.com
   Password: admin123
```

إذا رأيت هذه الرسائل، فالجداول مربوطة بشكل صحيح! 🎉

